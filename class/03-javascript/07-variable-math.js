//0~1 사이의 숫자
Math.random();

//소숫점 아래 버림
Math.floor();

//문자열로 변환
String();

// 현재 문자열의 앞에 다른 문자열을 채워, 주어진 길이를 만족하는 새로운 문자열을 반환
padStart();

String(Math.floor(Math.random() * 1000000)).padStart(6, "0");
